  <section id="questions">
    <h2>My Questions</h2>
    <table class="table table-striped table-hover table-condensed">
      <thead>
        <tr>
          <th class="text-center">Question</th>
          <th class="text-center">Industry</th>
          <th class="text-center">Answers</th>
          <th class="text-center">Likes</th>
          <th class="text-center">Status</th>
        </tr>
      </thead>
      <tbody>
        {{#each questionsAsked}}
          <tr>
            <td class="text-center"> <a href="{{this._id}}"> {{ this.question }} </a></td>
            <td class="text-center"> {{ this.industry }} </td>
            <td class="text-center"> {{ this.answers }} </td>
            <td> {{ this.likes }} </td>
            <td> {{ this.status }} </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </section>